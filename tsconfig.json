{
    "compilerOptions": {
        "allowJs": true,
        "lib": ["DOM", "es2017"],
        "target": "ES2017",
        "sourceMap": true,
        "baseUrl": ".", // This must be specified if "paths" is.
        "paths": {
            "*": ["dts/*", "*"]
        }
    }
}